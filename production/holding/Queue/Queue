Queue
    性质:典型的先进先出(FIFO)的容器.从容器的一端放入事物,从另一端取出,并且事物放入容器的顺序和取出的顺序是相同的.
    常被当作一种可靠的将对象从程序的一个区域传输到另一个区域的途径.队列在并发编程中很重要,它们可以安全地将对象从一个任务传输到另一个任务.
    LinkedList提供了方法来支持队列的行为,并且它实现了Queue接口,所以LinkedList可以用作Queue的一种实现.将LinkedList向上转型为Queue.
    方法:
        offer()
            在允许的情况下,将一个元素插入到队尾,或者返回false.
        peek()和element()
            在不移除的情况下,返回返回队头.在队列为空的时候,peek()返回null,element()放回NoSuchElementException异常.
        poll()和remove()
            移除并返回队头.在队列为空的时候,pull()返回null,remove()放回NoSuchElementException异常.

Priority(优先队列)
    性质:优先级队列声明下一个弹出元素是最需要的元素(优先级最高的元素).当你在PriorityQueue上调用offer()方法插入一个对象时,这个对象会在队列中排序
    (按照默认的排序方式,一般自定义).
    业务场景:
        1.在飞机场,当飞机起飞的时候,这架飞机的乘客可以在办理登记手续时排到队头.
        2.构建消息系统时,某些消息比其他消息更重要,因而应该更快地得到处理,那么它们何时处理和它们的到达无关.